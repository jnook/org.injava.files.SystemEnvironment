apply plugin: 'java'
apply plugin: 'eclipse'

group = 'org.injava.system'

sourceCompatibility = 1.2
targetCompatibility = 1.2

repositories {
  mavenCentral();
}

dependencies {
  compile project(':SystemProperties')
  compile 'org.injava.lang:DesignByContract:0'
}
